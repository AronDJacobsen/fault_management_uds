


hparam_key_paths:
  - 'training_args/max_epochs'
  - 'training_args/model_to_load'


training_args:
  max_epochs: 20 # range
  early_stopping_patience: 20 # avoid stopping early

  model_to_load: 'last_model_path' # ['best_model_path', 'last_model_path']



